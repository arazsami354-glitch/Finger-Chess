# Only used by vite.config.ts's dev-server proxy — points local /api
# requests at the backend during `npm run dev`. Not read at runtime by the
# built app; for production, serve this behind the same origin/reverse
# proxy as the backend so /api/* resolves without CORS.
VITE_FINGER_CHESS_API_PROXY_TARGET=http://localhost:3000
